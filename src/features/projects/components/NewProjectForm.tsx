import { Form, FormLabel } from "@/src/shared/components/forms";

export default function NewProjectForm() {
    return (
        <Form>
            <div>
                <FormLabel></FormLabel>
            </div>
        </Form>
    );
}
